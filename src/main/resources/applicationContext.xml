<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!--импортировать внешний файл значений-->
    <context:property-placeholder location="musicPlayer.properties"/>

    <!--создать новый бин (объект)-->
    <bean id="testBean"
          class="TestSpring.testBean">
        <constructor-arg value="Dima"/>
    </bean>
    <bean id="musicBean"
          class="Music.RockMusic">
    </bean>

    <!--импортировать бин в другой объект-->
    <bean id="musicPlayer"
          class="Music.MusicPlayer">
        <constructor-arg ref="musicBean"/>
        <!--setter (useless)<property name="music" ref="musicBean"/>-->
        <property name="volume" value="${musicPlayer.volume}"/>
    </bean>

</beans>